// Canonical product identity and release metadata. One definition for every host.
//
// This file is written so Gradle and Xcode can both consume it verbatim: every setting is
// `NAME=value`, and the only comment form is `//`, which xcconfig treats as a comment and a
// Java Properties reader parses as a key nothing asks for.
//
// Consumers:
//   - the root build.gradle.kts, which exposes these to :androidApp, :desktopApp and :shared
//   - iosApp/Configuration/Config.xcconfig, which #includes this file
//
// Versioning policy: docs/development/versioning.md

PRODUCT_NAME=Android Engineering Lab
MARKETING_VERSION=0.1.0
CURRENT_PROJECT_VERSION=1
